Super simple testing tool: send a packet over tcp with nodejs

Exactly the same as using netcat: `nc <host> <port> < test.json`

Install:
    `npm install -g node-tcp-push`

Use:
    `node-tcp-push <file> <port> <host | localhost>`

